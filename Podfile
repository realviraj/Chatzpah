project 'chatzpah8.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'chatzpah8' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for chatzpah8
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'Socket.IO-Client-Swift'

  target 'chatzpah8Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'chatzpah8UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
